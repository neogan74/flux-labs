apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: stage
namePrefix: stage-
commonLabels:
  app: podinfo
  env: stage
  owner: platform-team
  team: platform
resources:
  - ../../base
patches:
  - path: patch-deployment.yaml
